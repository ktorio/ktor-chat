product:
    type: jvm/app

apply:
    - ../server.module-template.yaml

dependencies:
    - ../../core
    - ../../db
    - ..
    - ../rest
    - $libs.postgresql

settings:
    jvm:
        mainClass: io.ktor.server.cio.EngineMain
    kotlin:
        serialization: json
