product:
    type: lib
    platforms: [jvm]

apply:
    - server.module-template.yaml

dependencies:
    - ../core
    - ../db
    - $ktor.server.callLogging
    - $libs.logback.classic

settings:
    ktor:
        enabled: true
#        version: 3.2.0-eap-1322
    kotlin:
        serialization: json
